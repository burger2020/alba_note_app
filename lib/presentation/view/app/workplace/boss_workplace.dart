import 'package:flutter/material.dart';

class BossWorkplace extends StatelessWidget {
  const BossWorkplace({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('boss'),
      ),
    );
  }
}
