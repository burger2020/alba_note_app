import 'package:flutter/material.dart';

class EmployeeWorkplace extends StatelessWidget {
  const EmployeeWorkplace({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('employee'),
      ),
    );
  }
}
